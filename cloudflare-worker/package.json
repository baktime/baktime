{
  "name": "baktime-cloudflare-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Schedule-aware dispatcher: reads a {name,type,schedule} manifest from KV and fires repository_dispatch for due targets. Holds no target details, no credentials.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cron-parser": "^5.0.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.120.0"
  }
}
