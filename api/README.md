# api/

Empty in this template — populated by the Phase 3 status generator
(`src/status/generate-api.ts`, not built yet), producing `summary.json` and
one `<target>.json` per target for the status site to read. See
`../ROADMAP.md`.
